# Python (backend)
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
.pytest_cache/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# Env / secrets
.env
.env.local
.env.*.local

# Logs / build artifacts
*.log
dist/
build/

# Everything the backend owns at runtime (config, state, logs, shared, trash, caches)
backend/data/
backend/cache/

# Session output a Playwright MCP attached to that browser leaves behind
backend/.playwright-mcp/

# Serena (project config, cache and memories)
.serena/
