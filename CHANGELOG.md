- The right panel opens full screen on the phone: the terminal, the files, the project, the monitor, the browser and a second chat
- The second chat takes over the window like the one you were on, and you move between the two from the tabs switcher
- A tool call that took more than a second says how long it took, off by default, with its own switch under token usage
- The projects you hide stay hidden everywhere, and so are their chats
- The project panel refreshes after the connection drops and its buttons to jump between changes stop blinking, the rewind button waits until the chat is ready, the press feedback lands on the shape you pressed, and the closed drawer leaves no hairline at the edge

> [!NOTE]
> The web version is available at https://app.cconnect.dev/
