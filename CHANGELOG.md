- An account you are already signed into can be signed in again, to refresh it without deleting anything
- Changing the model, effort, permissions or default account from any screen now reaches every open chat instead of only the next one
- The chat list stays where you left it when a conversation is created or deleted
- The project prompt and memories open on the environment's own project, named by its path
- A permission mode the server does not know no longer stops a new conversation from starting

> [!NOTE]
> The web version is available at https://cconnect.pages.dev/
