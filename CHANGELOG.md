- Chat visibility gains a simple mode that folds thinking and tool detail into a single working block, the way the quick chat already reads
- How much of each turn you see is now chosen per device on top of the server default, and changing it redraws the conversation you are looking at
- A server setting exposes the task-tracking tools that recent models otherwise leave out
- The quick chat follows the permission mode of the main chat instead of asking about everything
- An interrupted conversation stays listed and can be resumed
- Borders, shadows, spacing and the circular indicators match across every screen
- The app now also ships as a second build made with Tauri, released alongside this one as an alternative

> [!NOTE]
> The web version is available at https://cconnect.pages.dev/  
> The Tauri web version is available at https://cconnect-tauri.pages.dev/
