{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Permissions granted only on desktop builds",
  "platforms": ["linux", "macOS", "windows"],
  "windows": ["main"],
  "permissions": ["drag:default"]
}
